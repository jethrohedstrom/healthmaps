---
const currentPath = Astro.url.pathname;

const navLinks = [
  { href: '/pathway/', label: 'Getting Started' },
  { href: '/practitioners/', label: 'Compare Practitioners' },
  { href: '/calculator/', label: 'Cost Calculator' },
  { href: '/stories/', label: 'Stories' },
  { href: '/tips/', label: 'Tips' },
];
---
<header class="sticky top-0 z-50 bg-white border-b border-gray-200">
  <nav class="max-w-6xl mx-auto px-4 py-5" aria-label="Main navigation">
    <div class="flex items-center justify-between">
      <a href="/" class="-ml-1 text-xl font-bold text-brand-blue hover:text-brand-blue/80 transition-colors" aria-label="HealthMaps home">
        HealthMaps
      </a>

      <!-- Mobile menu button -->
      <button
        id="mobile-menu-btn"
        class="md:hidden p-2 text-gray-600 hover:text-brand-green"
        aria-expanded="false"
        aria-controls="mobile-menu"
        aria-label="Toggle navigation menu"
      >
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
          <path id="menu-icon" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>

      <!-- Desktop nav -->
      <div class="hidden md:flex items-center gap-6">
        <ul class="flex items-center gap-6 text-sm font-medium">
          {navLinks.map(link => (
            <li>
              <a
                href={link.href}
                class:list={[
                  'hover:text-brand-green transition-colors',
                  currentPath === link.href ? 'text-brand-green font-semibold' : 'text-gray-500',
                ]}
                aria-current={currentPath === link.href ? 'page' : undefined}
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
        <a
          href="https://chat.healthmaps.com.au"
          target="_blank"
          rel="noopener noreferrer"
          class="text-sm font-medium text-gray-500 hover:text-brand-green transition-colors"
        >
          Chat
        </a>
        <a
          href="/pathway/"
          class="bg-brand-green text-white px-5 py-2 rounded-lg text-sm font-semibold hover:bg-brand-green/90 transition-colors"
        >
          Get Started
        </a>
      </div>
    </div>

    <!-- Mobile nav -->
    <ul id="mobile-menu" class="md:hidden mt-4 space-y-1 text-base font-medium border-t border-gray-200 pt-4 max-h-0 opacity-0 overflow-hidden transition-all duration-200 ease-out">
      {navLinks.map(link => (
        <li>
          <a
            href={link.href}
            class:list={[
              'block py-3.5 px-4 rounded-lg text-center hover:bg-gray-50 transition-colors',
              currentPath === link.href ? 'text-brand-green font-semibold bg-gray-50' : 'text-gray-500',
            ]}
            aria-current={currentPath === link.href ? 'page' : undefined}
          >
            {link.label}
          </a>
        </li>
      ))}
      <li>
        <a
          href="https://chat.healthmaps.com.au"
          target="_blank"
          rel="noopener noreferrer"
          class="block py-3.5 px-4 rounded-lg text-center hover:bg-gray-50 transition-colors text-gray-500"
        >
          Chat
        </a>
      </li>
      <li class="pt-2">
        <a
          href="/pathway/"
          class="block text-center bg-brand-green text-white px-5 py-3.5 rounded-lg font-semibold hover:bg-brand-green/90 transition-colors"
        >
          Get Started
        </a>
      </li>
    </ul>
  </nav>
</header>

<script>
  const btn = document.getElementById('mobile-menu-btn');
  const menu = document.getElementById('mobile-menu');
  const icon = document.getElementById('menu-icon');
  if (btn && menu) {
    btn.addEventListener('click', () => {
      const expanded = btn.getAttribute('aria-expanded') === 'true';
      btn.setAttribute('aria-expanded', String(!expanded));
      if (expanded) {
        menu.classList.remove('max-h-[500px]', 'opacity-100');
        menu.classList.add('max-h-0', 'opacity-0');
      } else {
        menu.classList.remove('max-h-0', 'opacity-0');
        menu.classList.add('max-h-[500px]', 'opacity-100');
      }
      if (icon) {
        icon.setAttribute('d', expanded
          ? 'M4 6h16M4 12h16M4 18h16'
          : 'M6 18L18 6M6 6l12 12'
        );
      }
    });
  }
</script>
